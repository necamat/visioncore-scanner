namespace VisionCore.Domain.Imaging.Layout;

/// <summary>Identifies a region of the quiz form that the pipeline crops and reads.</summary>
public enum FormRegion
{
    TeamId,
    TeamIdDigit1,
    TeamIdDigit2,
    Score,
    ScoreDigit1,
    ScoreDigit2,
    ScoreDigit3
}
